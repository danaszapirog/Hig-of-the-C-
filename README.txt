

The start recruiting function is changing the state of the merchant one time time ahead but I was not able to figure out why.
The save command is working and the read command is generating all the correct objects but sometimes adds an additional pirate.

Everything else should be working correctly (or almost doing so).# High-of-the-C-s
